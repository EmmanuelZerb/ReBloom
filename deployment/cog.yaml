build:
  python_version: "3.10"
  python_packages:
    - numpy==1.24.3
    - torch==2.1.0
    - torchvision==0.16.0
    - pillow>=10.0.0
  gpu: true
  cuda: "11.8"
  run:
    - pip install numpy==1.24.3 --force-reinstall

predict: "predict.py:Predictor"
